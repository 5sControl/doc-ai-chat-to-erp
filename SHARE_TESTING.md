# 📱 Тестирование Share Функционала

## ✅ Что было сделано:

1. **Пакеты:** Перешли на `app_links: ^6.4.1`
2. **iOS:** Настроен MethodChannel + Share Extension
3. **Android:** Настроен MainActivity для Share Intents
4. **Flutter:** Реализована обработка всех типов share-контента

---

## 🧪 Как протестировать:

### iOS (Симулятор или Устройство)

#### 1. Установка и запуск:
```bash
cd /Users/mac/easably/g/docaichat/SummifyFlutter

# Установить pods
cd ios && pod install && cd ..

# Запустить на симуляторе
flutter run

# Или на реальном устройстве
flutter run -d <device-id>
```

#### 2. Тест Share из Safari:
1. Откройте Safari на устройстве/симуляторе
2. Перейдите на любую статью (например, https://example.com)
3. Нажмите кнопку Share (квадрат со стрелкой вверх)
4. Выберите **Summify** в списке
5. Нажмите Post

**Ожидаемое поведение:**
- Приложение откроется
- Начнется создание summary
- В логах увидите:
  ```
  🔥 Share Extension callback detected
  🔥 Found shared text/URLs: [url]
  🔥 Flutter: Shared text received
  🔥 Processing shared text: https://...
  🔥 Detected URL, processing summary...
  ```

#### 3. Просмотр логов iOS:
```bash
# В Xcode: Window → Devices and Simulators → Open Console
# Или в терминале для симулятора:
xcrun simctl spawn booted log stream --predicate 'subsystem contains "Summify"' | grep "🔥"

# Для реального устройства:
idevicesyslog | grep "🔥"
```

---

### Android (Эмулятор или Устройство)

#### 1. Установка и запуск:
```bash
cd /Users/mac/easably/g/docaichat/SummifyFlutter

# Запустить на эмуляторе
flutter run

# Или на устройстве
flutter run -d <device-id>
```

#### 2. Тест Share из Chrome:
1. Откройте Chrome на устройстве
2. Перейдите на любую статью
3. Нажмите меню (⋮) → Share
4. Выберите **Summify**

**Ожидаемое поведение:**
- Приложение откроется
- Начнется создание summary
- В Logcat увидите:
  ```
  🔥 MainActivity: handleIntent called
  🔥 Intent action: android.intent.action.SEND
  🔥 Shared text: https://...
  🔥 Flutter: Shared text received
  🔥 Processing shared text: https://...
  ```

#### 3. Просмотр логов Android:
```bash
# В терминале:
adb logcat | grep "🔥"

# Или в Android Studio: Logcat → Filter: "🔥" или "Summify"
```

---

## 📄 Тестирование документов

### iOS - Файлы из Files app:
1. Откройте приложение Files
2. Найдите PDF/Word документ
3. Долгое нажатие → Share → Summify

### Android - Файлы из любого файл-менеджера:
1. Откройте файл-менеджер (Files, Google Drive и т.д.)
2. Выберите документ → Share → Summify

**Ожидаемое поведение:**
- Приложение получит путь к файлу
- Начнется обработка документа
- В логах:
  ```
  🔥 Processing shared media: {path: /path/to/file.pdf, type: 2}
  🔥 File path: /path/to/file.pdf, fileName: file.pdf, type: 2
  ```

---

## 📝 Тестирование текста

### iOS - Текст из Notes/любого приложения:
1. Выделите текст в Notes или другом приложении
2. Share → Summify

### Android - Текст из любого приложения:
1. Выделите текст
2. Share → Summify

**Ожидаемое поведение:**
- Приложение получит выделенный текст
- Начнется создание summary из текста
- В логах:
  ```
  🔥 Processing shared text: [выделенный текст]
  🔥 Detected plain text
  ```

---

## 🐛 Отладка проблем

### Приложение не появляется в Share меню:

**iOS:**
1. Убедитесь что Share Extension настроен:
   ```bash
   ls -la ios/Share\ Extension/
   ```
2. Проверьте что в Xcode включен Share Extension target
3. Пересобрите приложение

**Android:**
1. Проверьте AndroidManifest.xml на наличие intent-фильтров
2. Переустановите приложение:
   ```bash
   flutter clean
   flutter run
   ```

### Приложение открывается, но ничего не происходит:

1. **Проверьте логи** (см. выше)
2. **Проверьте инициализацию блоков:**
   - Убедитесь что SummariesBloc получает данные
   - Проверьте что не падает в момент обработки
3. **Hot Restart:**
   ```bash
   # В терминале где запущен flutter run:
   # Нажмите R для hot restart
   ```

---

## 📊 Поддерживаемые форматы

| Тип контента | iOS | Android | Обработка |
|--------------|-----|---------|-----------|
| **URL** (http/https) | ✅ | ✅ | `GetSummaryFromUrl` |
| **Простой текст** | ✅ | ✅ | `GetSummaryFromText` |
| **PDF документы** | ✅ | ✅ | `GetSummaryFromFile` |
| **Word файлы** (.docx) | ✅ | ✅ | `GetSummaryFromFile` |
| **Текстовые файлы** (.txt) | ✅ | ✅ | `GetSummaryFromFile` |
| **Изображения** | ✅ | ✅ | `GetSummaryFromFile` |
| **Видео** | ✅ | ✅ | `GetSummaryFromFile` |

---

## 🔧 Быстрые команды

```bash
# Очистка и пересборка
flutter clean && flutter pub get && cd ios && pod install && cd .. && flutter run

# Только iOS
flutter run -d "iPhone 15 Pro"

# Только Android
flutter run -d emulator-5554

# Просмотр всех устройств
flutter devices

# Анализ кода
flutter analyze

# Проверка лог-файлов в реальном времени (iOS)
xcrun simctl spawn booted log stream | grep "🔥"

# Проверка лог-файлов в реальном времени (Android)
adb logcat -s "Summify:D" "flutter:I"
```

---

## ✨ Результаты миграции

- ❌ Удален: `receive_sharing_intent: ^1.8.1`
- ✅ Добавлен: `app_links: ^6.4.1`
- ✅ iOS: Полная интеграция с Share Extension через MethodChannel
- ✅ Android: Native обработка Share Intents
- ✅ Flutter: Универсальная обработка всех типов контента
- ✅ Логирование: 🔥 префикс для всех share-событий

---

## 📞 Дополнительная помощь

Если что-то не работает:
1. Проверьте логи с префиксом 🔥
2. Убедитесь что все пакеты установлены (`flutter pub get`)
3. Пересоберите приложение (`flutter clean && flutter run`)
4. Проверьте что используете правильный bundle ID для iOS
