import 'package:flutter/material.dart';

class RateSummaryScreen extends StatelessWidget {
  const RateSummaryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hello!'),
      ),
    );
  }
}
